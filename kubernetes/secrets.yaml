# This is a template. The actual JWT secret will be generated and applied by the deploy script
apiVersion: v1
kind: Secret
metadata:
  name: jwt-secret
  namespace: ethereum-node
type: Opaque
data:
  # This is a placeholder - the real JWT secret will be generated during deployment
  jwtsecret: {{ JWT_SECRET }}