---
# Main playbook for Ethereum node setup
- name: Provision Ethereum Node on Bare Metal
  hosts: ethereum_nodes
  become: true

  roles:
    - common          # Basic system setup and optimization
    - network         # Network configuration and firewall rules
    - kubernetes      # Kubernetes installation and configuration
    - ethereum        # Ethereum-specific dependencies and setup